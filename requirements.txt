# LAVARD runtime deps (Phase 1). Pinned loosely; lock before go-live.
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
typer>=0.12
rich>=13.7

# Optional / later phases (import-guarded so Phase 1 runs without them):
#   arq>=0.25            # async task queue (Redis) — Phase 1 queue, inline fallback if absent
#   qdrant-client>=1.9   # semantic memory + cache — Phases 6/7
#   psycopg[binary]>=3.1 # Postgres driver — set DATABASE_URL=postgresql+psycopg://...

# Dev/test
pytest>=8.0
pytest-asyncio>=0.23
